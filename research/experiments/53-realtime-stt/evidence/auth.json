{
  "boundary": "gateway only; the runtime is internal (same posture as /v1/transcribe)",
  "mechanism": "first WS message carries the API key; verified via the SAME AuthService pipeline as HTTP Bearer",
  "refusals": {
    "flag_off": "handshake refused pre-accept (probe: InvalidStatus/403; browser: friendly message)",
    "bad_key": "session.error invalid_api_key + close 4401 (unit-tested)",
    "unsupported_language": "session.error + close 4400 (unit-tested)"
  },
  "no_logging": "key never logged; audio and transcripts never logged (route reviewed + leak scans in browser E2E)"
}
