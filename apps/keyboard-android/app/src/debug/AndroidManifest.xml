<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG-ONLY manifest overlay: permits cleartext HTTP to the Android
     emulator's host loopback (10.0.2.2) so development builds can talk
     to a local IntelliAI stack. Release builds never merge this file —
     they keep the platform default of HTTPS-only. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
