"""HTTP binding — the contract's wire realization, and the ONLY transport-
aware layer in the runtime. Everything below this package speaks
contract-shaped data and `RuntimeFailure`; everything HTTP lives here."""
