# services/stt-whisper — Speech-to-Text (faster-whisper)

STT inference service wrapping faster-whisper (MIT; Whisper weights MIT).
Arrives in Milestone 2 (v0.3). CPU-first (int8), GPU via deployment config only.
