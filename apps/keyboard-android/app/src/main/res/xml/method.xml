<?xml version="1.0" encoding="utf-8"?>
<!-- The IME's system-facing declaration. settingsActivity points at the
     setup screen so Android's keyboard settings deep-link back to us. -->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="com.intelliai.keyboard.setup.MainActivity"
    android:supportsSwitchingToNextInputMethod="true">
    <subtype
        android:label="@string/subtype_english"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard" />
</input-method>
